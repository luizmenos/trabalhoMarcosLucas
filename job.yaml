apiVersion: batch/v1
kind: Job
metadata:
  name: gerar-imagens-job
spec:
  template:
    spec:
      restartPolicy: OnFailure
      containers:
      - name: gerar-imagens
        image: gerar-imagens:latest
        command: ["python", "/app/gerar_imagens.py"]
        volumeMounts:
        - name: input-volume
          mountPath: /app/input
      volumes:
      - name: input-volume
        persistentVolumeClaim:
          claimName: input-pvc
